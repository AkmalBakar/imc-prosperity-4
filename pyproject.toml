[project]
name = "prosperity4"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
research = [
    "matplotlib",
    "numpy",
    "pandas",
    "ipykernel",
    "statsmodels",
]
viz = [
    "dash",
    "plotly",
]
